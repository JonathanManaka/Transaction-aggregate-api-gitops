# Overrides for the vendored upstream Grafana chart (see Chart.yaml).
# Values are nested under the dependency name 'grafana'. Anything omitted
# uses the chart defaults.
grafana:

  envValueFrom:
    GF_SECURITY_ADMIN_PASSWORD:
      secretKeyRef:
        name: grafana-secret
        key: grafana-admin-password

  persistence:
    enabled: false

  service:
    type: NodePort
    nodePort: 30300          # Grafana UI at http://localhost:30300

  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 256Mi

  datasources:
    datasources.yaml:
      apiVersion: 1
      datasources:
        - name: Prometheus
          type: prometheus
          access: proxy
          url: http://prometheus-server.prometheus.svc.cluster.local
          isDefault: true
